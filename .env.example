JBM_ENV=development
JBM_HTTP_ADDR=:8080
JBM_HTTP_READ_HEADER_TIMEOUT=5s
JBM_HTTP_READ_TIMEOUT=10s
JBM_HTTP_WRITE_TIMEOUT=15s
JBM_HTTP_IDLE_TIMEOUT=60s
JBM_HTTP_SHUTDOWN_TIMEOUT=10s

JBM_DATABASE_URL=postgres://postgres:postgres@localhost:5432/justbarme?sslmode=disable
JBM_DB_MAX_CONNS=10
JBM_DB_MIN_CONNS=1
JBM_DB_MAX_CONN_LIFETIME=30m
JBM_DB_MAX_CONN_IDLE_TIME=5m
JBM_DB_HEALTH_TIMEOUT=2s

JBM_LOG_LEVEL=info

# Online session (opaque cookie) — docs/API_CONTRACT.md §4
JBM_SESSION_TTL=720h
# Must be true in staging/production; only relax for HTTPS-less local dev.
JBM_SESSION_COOKIE_SECURE=true

# Password hashing (Argon2id) — tune to your deployment hardware
JBM_ARGON2_MEMORY_KIB=65536
JBM_ARGON2_ITERATIONS=3
JBM_ARGON2_PARALLELISM=2

# Offline authorization lease (Ed25519) — docs/ARCHITECTURE.md §11.2
# Required in production. Generate a pair with crypto/ed25519.GenerateKey
# and base64.StdEncoding-encode both keys. Left blank outside production, an
# ephemeral development-only keypair is generated at startup instead
# (invalidating any previously issued lease on every restart).
JBM_OFFLINE_LEASE_TTL=168h
JBM_OFFLINE_SIGNING_PRIVATE_KEY=
JBM_OFFLINE_SIGNING_PUBLIC_KEY=

# The frontend's own origin -- NOT this API's address. internal/invitations
# builds invitation links as "{JBM_PUBLIC_BASE_URL}/invite/{token}", and
# /invite/:token is a React Router route the Vite frontend serves, not an
# API endpoint; pointing this at the backend's own port produces a link
# that 404s. Locally this is the Vite dev server's origin
# (http://localhost:5173, its fixed dev port -- see web/vite.config.ts),
# not JBM_HTTP_ADDR's port. Must be https:// in production.
JBM_PUBLIC_BASE_URL=

# Transactional email (internal/email) for signup OTP delivery. Required in
# production. Gmail: use an app password (myaccount.google.com/apppasswords),
# not the account password -- Gmail rejects the account password over SMTP.
# Left blank outside production, signup codes are only logged, not emailed.
JBM_SMTP_HOST=smtp.gmail.com
JBM_SMTP_PORT=587
JBM_SMTP_USERNAME=
JBM_SMTP_PASSWORD=
JBM_SMTP_FROM=

# A failed send is retried this many times total (the original attempt plus
# retries), with exponential backoff starting at the base delay below.
# Applies only to the real SMTP provider -- the console dev fallback never
# fails, so there is nothing to retry outside production.
JBM_SMTP_MAX_SEND_ATTEMPTS=3
JBM_SMTP_RETRY_BASE_DELAY=500ms

# How long a signup verification code stays valid.
JBM_SIGNUP_OTP_TTL=10m

# WhatsApp messaging (internal/whatsapp, docs/PHASE_INVITATIONS_WHATSAPP.md)
# for staff invitations and WhatsApp-channel signup OTPs, via Zavu. Required
# in production, same enforcement as SMTP above. Left blank outside
# production, messages are only logged, not sent (whatsapp.ConsoleProvider).
JBM_ZAVU_API_KEY=
JBM_ZAVU_SENDER_ID=
JBM_ZAVU_WEBHOOK_SECRET=

# Same retry semantics as JBM_SMTP_MAX_SEND_ATTEMPTS/RETRY_BASE_DELAY above,
# applied only to the real Zavu provider.
JBM_ZAVU_MAX_SEND_ATTEMPTS=3
JBM_ZAVU_RETRY_BASE_DELAY=500ms

# Optional: "Sign in with Google" (internal/oauth). Leave unset to disable
# it entirely -- POST /auth/google is simply not registered, unlike SMTP
# there is no dev fallback and no production requirement for this. Create
# an OAuth 2.0 Client ID of type "Web application" in Google Cloud Console
# (console.cloud.google.com/apis/credentials); this is a public identifier,
# not a secret -- the frontend needs the same value as
# VITE_GOOGLE_OAUTH_CLIENT_ID (web/.env). No client secret is needed: this
# backend only verifies ID tokens issued client-side, it never talks to
# Google's token endpoint itself.
JBM_GOOGLE_OAUTH_CLIENT_ID=

# Object storage (internal/storage, docs/PHASE_PILOT_RELEASE.md §3) for
# business logo uploads. The pilot points this at a Cloudflare R2 bucket
# via the generic S3-compatible client -- nothing here is R2-specific, a
# Backblaze B2 bucket works unchanged. Required in production, same
# enforcement as SMTP/Zavu above. Left blank outside production, uploads
# are written to JBM_STORAGE_LOCAL_DIR and served back by this API itself
# at /dev-uploads (storage.LocalDiskProvider) -- see internal/app.storageProvider.
JBM_STORAGE_ENDPOINT=
JBM_STORAGE_REGION=auto
JBM_STORAGE_ACCESS_KEY_ID=
JBM_STORAGE_SECRET_ACCESS_KEY=
JBM_STORAGE_BUCKET=
# Where uploaded objects are actually reachable from (a bucket's public dev
# URL, or a custom domain in front of it) -- deliberately separate from
# JBM_STORAGE_ENDPOINT, since the upload endpoint and the public read path
# are often different hosts.
JBM_STORAGE_PUBLIC_BASE_URL=
JBM_STORAGE_LOCAL_DIR=.dev-uploads

# Optional: gates GET /metrics (docs/PHASE_PILOT_RELEASE.md §6) behind
# "Authorization: Bearer <token>". Left blank, the endpoint is open --
# fine for local development. Not required in production (a leaked scrape
# token exposes aggregate counts, not tenant data, unlike SMTP/Zavu/
# Storage credentials above), but recommended once a real external
# scraper is polling it over the internet.
JBM_METRICS_TOKEN=
